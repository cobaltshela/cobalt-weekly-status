name: Verify weekly status

on:
  push:
    branches: [main]
  pull_request:

jobs:
  verify:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Check PDF action
        run: python3 scripts/verify-weekly-status.py
